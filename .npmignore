# Dev-only — not needed in the published npm package
vendor/
docs/
tests/
/design/
# Root-level openspec change tracking (not the extension)
/openspec/
examples/
scripts/
config/
.git/
.github/
.gitmodules
.pi/
*.test.ts
tsconfig.json
CONTRIBUTING.md
CHANGELOG.md
bootstrap.sh
auth.json

# Dev binaries (rg/fd are for bootstrap, not runtime)
bin/rg
bin/fd
bin/deploy

# Extension-local node_modules (installed at runtime by bootstrap)
extensions/render/composition/node_modules/
extensions/render/excalidraw/node_modules/
