# Memory branch-aware facts transport — isolate tracked facts.jsonl intent from branch-local runtime drift — Design Tasks

## 1. Design exploration

- [ ] 1.1 Explore and decide: Memory branch-aware facts transport — isolate tracked facts.jsonl intent from branch-local runtime drift
