# Localhost Read-Only Web UI MVP

## Intent

Add a first-party pi-kit web UI extension that serves a localhost-only read-only dashboard over HTTP using a versioned ControlPlaneState snapshot built from live shared state plus on-demand scans, with no mutation endpoints and no separate persistence layer.

## Scope

<!-- Define what is in scope and out of scope -->

## Success Criteria

<!-- How will we know this change is complete and correct? -->
