# Remotion native support via render extension

## Intent

Explore whether the existing render extension (Playwright/Chromium already present, D2/native/excalidraw pipeline established) can be extended to support Remotion video composition natively — letting the agent generate data-driven video from React components without leaving the pi session.
