# Cross-provider session telemetry schema for replay and inspection — Design Tasks

## 1. Open Questions

- [ ] 1.1 Should cleave child execution keep using spawned CLI subprocesses with stderr scraping, or move to a structured event/RPC channel with semantic heartbeats and typed progress events?
