# lifecycle-artifact-versioning — Tasks

## 1. Durable lifecycle artifacts are version controlled

- [x] 1.1 Design-tree and OpenSpec files are treated as durable artifacts
- [x] 1.2 Write tests for Durable lifecycle artifacts are version controlled

## 2. Transient cleave runtime artifacts remain optional

- [x] 2.1 Transient cleave workspace outputs are excluded from the durability requirement
- [x] 2.2 Write tests for Transient cleave runtime artifacts remain optional

## 3. Repository checks detect untracked durable lifecycle artifacts

- [x] 3.1 Untracked OpenSpec or design-tree files are surfaced by automated checks
- [x] 3.2 Write tests for Repository checks detect untracked durable lifecycle artifacts
