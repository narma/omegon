# lifecycle-artifact-versioning — Design

## Spec-Derived Architecture

### lifecycle/versioning

- **Durable lifecycle artifacts are version controlled** (added) — 1 scenarios
- **Transient cleave runtime artifacts remain optional** (added) — 1 scenarios
- **Repository checks detect untracked durable lifecycle artifacts** (added) — 1 scenarios

## Scope

<!-- Define what is in scope and out of scope -->

## File Changes

<!-- Add file changes as you design the implementation -->
