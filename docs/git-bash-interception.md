---
id: git-bash-interception
title: Bash tool git command interception strategy
status: seed
parent: git-harness-integration
open_questions:
  - "Should the bash tool intercept git commit/add/stash commands and redirect to the structured commit tool, or should it hard-block them?"
---

# Bash tool git command interception strategy

## Overview

> Parent: [Git as first-class harness citizen — commit hygiene and repo-aware lifecycle](git-harness-integration.md)
> Spawned from: "Should the bash tool intercept git commit/add/stash commands and redirect to the structured commit tool, or should it hard-block them?"

*To be explored.*

## Open Questions

- Should the bash tool intercept git commit/add/stash commands and redirect to the structured commit tool, or should it hard-block them?
